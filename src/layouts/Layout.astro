---
import '../styles/global.css';
import { TEMPLATE_CONFIG } from '../config';

const siteIcon = '/favicon.ico';

const {
	title = TEMPLATE_CONFIG.site.title,
	description = TEMPLATE_CONFIG.site.description,
} = Astro.props;

const navItems = [
	{ href: '/', label: 'Home' },
	{ href: '/article', label: 'Article' },
	{ href: '/about', label: 'About' },
	{ href: '/policy', label: 'Policy' },
	{ href: '/', label: 'Contact' },
];
---

<!doctype html>
<html lang={TEMPLATE_CONFIG.site.language}>
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<meta name="generator" content={Astro.generator} />
		<meta name="description" content={description} />
		<meta name="theme-color" content="#0f172a" />
		<title>{title}</title>
		<link rel="icon" href={siteIcon} />
	</head>
	<body>
		<header class="site-header">
			<div class="site-header-inner">
				<a class="site-brand" href="/" aria-label={`${TEMPLATE_CONFIG.site.title} home`}>
					<img src={siteIcon} alt={TEMPLATE_CONFIG.site.title} class="site-brand-mark" />
					<span>
						<strong>{TEMPLATE_CONFIG.site.title}</strong>
						<small>Starter Template</small>
					</span>
				</a>

				<nav class="site-nav" aria-label="Primary navigation">
					{navItems.map((item) => (
						<a
							href={item.href}
							target={item.href.startsWith('http') ? '_blank' : undefined}
							rel={item.href.startsWith('http') ? 'noreferrer' : undefined}
						>
							{item.label}
						</a>
					))}
				</nav>
			</div>
		</header>

		<main class="site-main">
			<slot />
		</main>

		<footer class="site-footer">
			<div class="site-footer-inner">
				<div>
					<p class="eyebrow">Additional Links</p>
					<div class="footer-links">
						{navItems.map((item) => (
							<a
								href={item.href}
								target={item.href.startsWith('http') ? '_blank' : undefined}
								rel={item.href.startsWith('http') ? 'noreferrer' : undefined}
							>
								{item.label}
							</a>
						))}
					</div>
				</div>
				<div>
					<p class="site-footer-note">© 2026 {TEMPLATE_CONFIG.site.title}</p>
					<p class="site-footer-note">
						This website is forked from
						<a href="https://github.com/hokke414/AstroBlogTemplate" target="_blank" rel="noreferrer">this repo</a>.
					</p>
				</div>
			</div>
		</footer>
	</body>
</html>
